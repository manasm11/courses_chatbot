This requires creds.json from dialogflow api from bot-test project.

To sun the project
1. Install requirements by `pip install -r requirements.txt` (preferably in a virtual environment.
2. Then run python start
